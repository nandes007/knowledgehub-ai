# env
.env
.env.local

# local podman override (rootless CNI dnsname workaround, machine-specific)
docker-compose.override.yml

# deployment secrets (vault.yml is encrypted, but don't tempt fate)
deploy/ansible/vault.yml
deploy/ansible/certs/*
!deploy/ansible/certs/.gitkeep

# python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# node
node_modules/
.next/
out/

# data volumes (local dev)
backend/.chroma/
backend/uploads/

# editors / os
.DS_Store
.idea/
.vscode/
.claude